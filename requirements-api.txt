# Slim requirements for the API-only Docker container.
# Excludes heavy vision deps (openvino, ultralytics, nncf, opencv, scipy).
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.0
pydantic-settings>=2.0.0
websockets>=13.0
python-multipart>=0.0.12
aiosqlite>=0.20.0
httpx>=0.27.0
